import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: '#b388ff',
    },
    secondary: {
      main: '#d1c4e9',
    },
  },
});

export default theme;